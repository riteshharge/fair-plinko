{
  "name": "plinko-backend-prisma",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node -r dotenv/config index.js",
    "start": "node -r dotenv/config index.js",
    "prepare": "echo Skipping chmod for Windows",
    "postinstall": "npm run prepare && npx prisma generate && npx prisma migrate deploy",
    "test": "node test/test_vector.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
